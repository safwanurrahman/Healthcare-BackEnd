# .env

PORT=5000

# MySQL Database Configuration (Replace placeholders with your actual details)
DB_HOST=localhost
DB_USER=root           # e.g., root, or your specific database user
DB_PASSWORD=your_password # YOUR MySQL password
DB_NAME=mediconnect    # The name of the database you will use (must be created in MySQL)
DB_DIALECT=mysql